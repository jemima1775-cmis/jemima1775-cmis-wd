jemima1775-cmis
ls=list folder
git clone --config="http.proxy=http://jemima1775:elljem7@192.168.2.1:8080" https://github.com/jemima1775-cmis/jemima1775-cmis-wd.git
gedit file & = open something in gedit
cd= enter into a folder
cd .. = go back to parent folder
git status= list recent changes
git add filename = added file
git commit -m “comment”
ctrl c = cancel command
git push= git push
clear=clear screen
sftp jemima1775@cmis-hsteacher = open sftp
put filename = move to public folder 
rm filename = remove 
!ls = shows whats on local machine
* = do it to all (ls * = list all)
*.txt = do it to all .txt files (works with others too)
python -m SimpleHTTPServer= start development server
exit = leave sftp
